# Requirements optimizado para Windows (wheels precompilados)
# Usar este archivo si requirements.txt falla con errores de compilación
# Ubicación: backend/requirements_windows.txt
# Instalar con: pip install -r backend/requirements_windows.txt

# Core Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic>=2.10,<3
pydantic-settings>=2.5,<3

# Database - sin paquetes que requieran compilación en Windows
sqlalchemy==2.0.25
psycopg[binary]>=3.1.18
alembic==1.13.1

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-multipart==0.0.6

# Utilities
python-dotenv==1.0.0
qrcode[pil]==7.4.2
